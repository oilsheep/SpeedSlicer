我想做一個產品

1.會自動去除掉輸入圖片的底色，然後切分出上面的UI元件，分別存成帶透明度的PNG檔案

2. 會自動偵測底色

3. 會處理透明度問題，例如，如果底色是綠色，那淺綠色的材質會變成半透明白色

4. 有一個簡單的UI介面，可以輸入圖片，並且處理完後，在切圖前會預先讓使用者預覽要切圖的切割樣子，可以手動調整去背的顏色以及每個元件的切割邊緣

5. 參考輸入範例在 ./test-file

